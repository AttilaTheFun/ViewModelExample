import ContentFoundation
import UIKit

// MARK: MetadataClosure

/**
 A closure that takes no arguments and is embedded in the metadata dictionary.
 */
public typealias MetadataClosure = () -> Void

// MARK: MetadataKey

/**
 A namespace for metadata closure dictionary keys.
 */
public enum MetadataKey {}
